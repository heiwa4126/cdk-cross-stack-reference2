{
	"name": "cdk-cross-stack-reference2",
	"version": "0.1.0",
	"bin": {
		"cdk-cross-stack-reference2": "bin/cdk-cross-stack-reference2.js"
	},
	"scripts": {
		"preinstall": "pnpm audit",
		"format": "oxfmt",
		"lint": "oxlint",
		"check": "oxfmt && oxlint --fix",
		"test": "jest",
		"bootstrap": ". ./.env && cdk bootstrap",
		"list": "cdk list",
		"synth": "cdk synth",
		"deploy": ". ./.env && cdk deploy --all --require-approval never",
		"diff": ". ./.env && cdk diff --all",
		"destroy": ". ./.env && cdk destroy --all",
		"build": "tsc",
		"watch": "tsc -w",
		"cdk": "cdk"
	},
	"dependencies": {
		"aws-cdk-lib": "2.267.0",
		"constructs": "^10.8.1"
	},
	"devDependencies": {
		"@types/jest": "^30.0.0",
		"@types/node": "26.4.0",
		"aws-cdk": "2.1139.0",
		"jest": "^30.5.1",
		"ts-jest": "^29.4.12",
		"ts-node": "^10.9.2",
		"tsx": "^4.23.13",
		"typescript": "~7.0.2"
	}
}
